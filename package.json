{
  "name": "pawfect",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:api": "nodemon --watch server.js --watch .env server.js",
    "dev:full": "concurrently -n \"API,Vite\" -c \"yellow,cyan\" \"npm run dev:api\" \"npm run dev\"",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "nodemon": "^3.1.14",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "concurrently": "^9.2.1",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "three": "^0.164.1"
  }
}
